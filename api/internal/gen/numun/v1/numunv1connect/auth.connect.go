// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: numun/v1/auth.proto

package numunv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/numun/numun/api/internal/gen/numun/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// AuthServiceName is the fully-qualified name of the AuthService service.
	AuthServiceName = "numun.v1.AuthService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// AuthServiceExchangeProcedure is the fully-qualified name of the AuthService's Exchange RPC.
	AuthServiceExchangeProcedure = "/numun.v1.AuthService/Exchange"
	// AuthServiceLogoutProcedure is the fully-qualified name of the AuthService's Logout RPC.
	AuthServiceLogoutProcedure = "/numun.v1.AuthService/Logout"
	// AuthServiceRecordPasswordResetCompletedProcedure is the fully-qualified name of the AuthService's
	// RecordPasswordResetCompleted RPC.
	AuthServiceRecordPasswordResetCompletedProcedure = "/numun.v1.AuthService/RecordPasswordResetCompleted"
)

// AuthServiceClient is a client for the numun.v1.AuthService service.
type AuthServiceClient interface {
	// Exchanges Cognito tokens (returned by the portal's direct SDK call) for
	// the opaque `numun_session` HttpOnly cookie + `csrf_token` cookie. No prior
	// session is required — this *is* the auth handshake.
	Exchange(context.Context, *connect.Request[v1.ExchangeRequest]) (*connect.Response[v1.ExchangeResponse], error)
	// Invalidates the current session: revokes the refresh token at Cognito
	// (best-effort), deletes the Session row, sets cookies to expire.
	Logout(context.Context, *connect.Request[v1.LogoutRequest]) (*connect.Response[v1.LogoutResponse], error)
	// Records a `password_reset_completed` AuthAuditEvent for the caller.
	// Cognito does not emit a native trigger for ConfirmForgotPassword, so the
	// portal calls this RPC after a successful password reset. See AUTH.md §10.1
	// and IMPLEMENTATION_PLAN.md ambiguity #9.
	//
	// Unauthenticated (the user is mid-recovery and has no session yet); the
	// server identifies the subject via the email parameter, which it must
	// resolve against Cognito before writing the event.
	RecordPasswordResetCompleted(context.Context, *connect.Request[v1.RecordPasswordResetCompletedRequest]) (*connect.Response[v1.RecordPasswordResetCompletedResponse], error)
}

// NewAuthServiceClient constructs a client for the numun.v1.AuthService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewAuthServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) AuthServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	authServiceMethods := v1.File_numun_v1_auth_proto.Services().ByName("AuthService").Methods()
	return &authServiceClient{
		exchange: connect.NewClient[v1.ExchangeRequest, v1.ExchangeResponse](
			httpClient,
			baseURL+AuthServiceExchangeProcedure,
			connect.WithSchema(authServiceMethods.ByName("Exchange")),
			connect.WithClientOptions(opts...),
		),
		logout: connect.NewClient[v1.LogoutRequest, v1.LogoutResponse](
			httpClient,
			baseURL+AuthServiceLogoutProcedure,
			connect.WithSchema(authServiceMethods.ByName("Logout")),
			connect.WithClientOptions(opts...),
		),
		recordPasswordResetCompleted: connect.NewClient[v1.RecordPasswordResetCompletedRequest, v1.RecordPasswordResetCompletedResponse](
			httpClient,
			baseURL+AuthServiceRecordPasswordResetCompletedProcedure,
			connect.WithSchema(authServiceMethods.ByName("RecordPasswordResetCompleted")),
			connect.WithClientOptions(opts...),
		),
	}
}

// authServiceClient implements AuthServiceClient.
type authServiceClient struct {
	exchange                     *connect.Client[v1.ExchangeRequest, v1.ExchangeResponse]
	logout                       *connect.Client[v1.LogoutRequest, v1.LogoutResponse]
	recordPasswordResetCompleted *connect.Client[v1.RecordPasswordResetCompletedRequest, v1.RecordPasswordResetCompletedResponse]
}

// Exchange calls numun.v1.AuthService.Exchange.
func (c *authServiceClient) Exchange(ctx context.Context, req *connect.Request[v1.ExchangeRequest]) (*connect.Response[v1.ExchangeResponse], error) {
	return c.exchange.CallUnary(ctx, req)
}

// Logout calls numun.v1.AuthService.Logout.
func (c *authServiceClient) Logout(ctx context.Context, req *connect.Request[v1.LogoutRequest]) (*connect.Response[v1.LogoutResponse], error) {
	return c.logout.CallUnary(ctx, req)
}

// RecordPasswordResetCompleted calls numun.v1.AuthService.RecordPasswordResetCompleted.
func (c *authServiceClient) RecordPasswordResetCompleted(ctx context.Context, req *connect.Request[v1.RecordPasswordResetCompletedRequest]) (*connect.Response[v1.RecordPasswordResetCompletedResponse], error) {
	return c.recordPasswordResetCompleted.CallUnary(ctx, req)
}

// AuthServiceHandler is an implementation of the numun.v1.AuthService service.
type AuthServiceHandler interface {
	// Exchanges Cognito tokens (returned by the portal's direct SDK call) for
	// the opaque `numun_session` HttpOnly cookie + `csrf_token` cookie. No prior
	// session is required — this *is* the auth handshake.
	Exchange(context.Context, *connect.Request[v1.ExchangeRequest]) (*connect.Response[v1.ExchangeResponse], error)
	// Invalidates the current session: revokes the refresh token at Cognito
	// (best-effort), deletes the Session row, sets cookies to expire.
	Logout(context.Context, *connect.Request[v1.LogoutRequest]) (*connect.Response[v1.LogoutResponse], error)
	// Records a `password_reset_completed` AuthAuditEvent for the caller.
	// Cognito does not emit a native trigger for ConfirmForgotPassword, so the
	// portal calls this RPC after a successful password reset. See AUTH.md §10.1
	// and IMPLEMENTATION_PLAN.md ambiguity #9.
	//
	// Unauthenticated (the user is mid-recovery and has no session yet); the
	// server identifies the subject via the email parameter, which it must
	// resolve against Cognito before writing the event.
	RecordPasswordResetCompleted(context.Context, *connect.Request[v1.RecordPasswordResetCompletedRequest]) (*connect.Response[v1.RecordPasswordResetCompletedResponse], error)
}

// NewAuthServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewAuthServiceHandler(svc AuthServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	authServiceMethods := v1.File_numun_v1_auth_proto.Services().ByName("AuthService").Methods()
	authServiceExchangeHandler := connect.NewUnaryHandler(
		AuthServiceExchangeProcedure,
		svc.Exchange,
		connect.WithSchema(authServiceMethods.ByName("Exchange")),
		connect.WithHandlerOptions(opts...),
	)
	authServiceLogoutHandler := connect.NewUnaryHandler(
		AuthServiceLogoutProcedure,
		svc.Logout,
		connect.WithSchema(authServiceMethods.ByName("Logout")),
		connect.WithHandlerOptions(opts...),
	)
	authServiceRecordPasswordResetCompletedHandler := connect.NewUnaryHandler(
		AuthServiceRecordPasswordResetCompletedProcedure,
		svc.RecordPasswordResetCompleted,
		connect.WithSchema(authServiceMethods.ByName("RecordPasswordResetCompleted")),
		connect.WithHandlerOptions(opts...),
	)
	return "/numun.v1.AuthService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case AuthServiceExchangeProcedure:
			authServiceExchangeHandler.ServeHTTP(w, r)
		case AuthServiceLogoutProcedure:
			authServiceLogoutHandler.ServeHTTP(w, r)
		case AuthServiceRecordPasswordResetCompletedProcedure:
			authServiceRecordPasswordResetCompletedHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedAuthServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedAuthServiceHandler struct{}

func (UnimplementedAuthServiceHandler) Exchange(context.Context, *connect.Request[v1.ExchangeRequest]) (*connect.Response[v1.ExchangeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("numun.v1.AuthService.Exchange is not implemented"))
}

func (UnimplementedAuthServiceHandler) Logout(context.Context, *connect.Request[v1.LogoutRequest]) (*connect.Response[v1.LogoutResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("numun.v1.AuthService.Logout is not implemented"))
}

func (UnimplementedAuthServiceHandler) RecordPasswordResetCompleted(context.Context, *connect.Request[v1.RecordPasswordResetCompletedRequest]) (*connect.Response[v1.RecordPasswordResetCompletedResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("numun.v1.AuthService.RecordPasswordResetCompleted is not implemented"))
}
