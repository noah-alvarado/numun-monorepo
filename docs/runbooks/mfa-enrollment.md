# Runbook — MFA enrollment (TOTP)

**Status:** stub. Created in M2 (Cognito user pool live).

Until the portal MFA UI ships in v1.1, this runbook documents how staff self-enroll TOTP via the AWS CLI (`aws cognito-idp associate-software-token` and `verify-software-token`). Staff-only. See AUTH.md §4.6 and SECURITY.md §4.2.
