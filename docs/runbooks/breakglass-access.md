# Runbook — Break-glass IAM access

**Status:** stub. Created in M1 when the `numun-break-glass` IAM user exists.

How to use the break-glass IAM user (hardware MFA required; no long-lived access keys; STS-temporary credentials only) during incidents that require admin-level AWS access outside the normal OIDC-federated CI path. When to use vs. not to. See SECURITY.md §3.2, §6.5.
