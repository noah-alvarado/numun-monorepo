# Runbook — Landing-page defacement

**Status:** stub. Filled out in M12 (depends on M5).

How to revert a malicious or accidental Decap CMS commit (`git revert`), trigger an emergency S3 re-sync from a prior version (S3 versioning is on for `numun-org-site`; 90-day retention), and review GitHub audit logs to identify the responsible collaborator. See SECURITY.md §3.1, §6.4.
