# NUMUN Decap CMS config — placeholder for M0.
#
# Full content-model + OAuth wiring lands in M5 per
# IMPLEMENTATION_PLAN.md and CMS_CONTENT_MODEL.md.

backend:
  name: github
  repo: numun/numun-vClaude # update once the repo is created
  branch: main
  base_url: https://api.test.numun.org # OAuth proxy lives on the Lambdalith (staging)
  auth_endpoint: cms-oauth/auth

publish_mode: simple
media_folder: "content/uploads"
public_folder: "/uploads"

collections: [] # collections defined in M5
